import { styled } from "styled-components";

const DashboardContainer = styled.div`
  background: #f0f2f5;
  height: 100vh;
`;

const Dashboard = () => {
  return (
    <DashboardContainer>
      <h1>Dashboard</h1>
    </DashboardContainer>
  );
};

export default Dashboard;
